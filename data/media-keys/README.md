# Media receipt verification keys

This directory contains public verification keys for Lumina Studio media
receipts. Public keys are not credentials and cannot create receipts. A key ID
in a contribution record must match the filename `<key-id>.pem`.

Only maintainers add or retire these files. Contributors should use the receipt
exactly as provided by the media handoff page.

---
id: reference-navigation-and-status
title: "导航、语言、主题与连接状态"
slug: "/reference/workspace-and-navigation/navigation-and-status"
description: "说明 Lumina Studio 顶部导航、二级页面、语言与主题切换、后端连接状态和更新公告的实际作用。"
sidebar_position: 1
---

# 导航、语言、主题与连接状态

Lumina Studio 的顶栏分成三部分：软件标识、功能导航和全局状态。切换页面不会自动开始预览、生成或保存，
但正在编辑的页面状态通常会继续保留。

<img
  src={require('./assets/01-header-navigation.webp').default}
  alt="Lumina Studio 中文界面的顶部导航、全局控件和后端连接指示"
/>

## 一级页面

| 页面 | 主要用途 |
| --- | --- |
| 图像转换 | 上传图片，选择颜色来源和结构参数，预览并生成 3MF |
| LUT 管理 | 管理 LUT、制作校准板、从照片提取 LUT、分析和比较 LUT |
| 耗材管理 | 管理耗材档案、生成或提取梯度卡、查询叠层配方 |
| 图像转矢量 | 把位图转换成可继续编辑或发送到图像转换页面的 SVG |
| 设置 | 选择机器、喷嘴和切片软件，并管理计算、缓存与界面选项 |

“LUT 管理”和“耗材管理”下面还有各自的二级页面。再次进入这两个一级页面时，软件会保留当前有效的二级
页面；若当前记录已经失效，则回到该模块的默认页面。

## 图像转换的三个二级页面

- **核心设置：** 图片、来源、尺寸、底板、模式、常用高级选项和操作栏；
- **颜色调整：** 2D 预览、选区、颜色替换、配方和颜色分组；
- **高级功能：** 浮雕、外轮廓、掐丝、透明镀层、挂件环、磁铁槽和拼图等结构功能。

核心设置和高级功能使用可移动面板；颜色调整由全屏颜色工作台接管，因此顶栏中的“面板控件”只会列出
当前页面实际存在的面板。详细行为见[面板显示、折叠、拖动与重置](../panel-layout/panel-layout.mdx)。

## 语言与主题

语言按钮在中文界面显示 `EN`，在英文界面显示 `中`。切换后，当前页面、输入值和预览不会重置；界面文字
改用另一种语言。

主题按钮在浅色和深色主题之间切换。主题只改变显示，不改变图片、配方、模型或导出文件。界面布局较窄时，
部分文字标签会收起，只保留图标；一级和二级导航可以横向滚动。

## 连接状态

顶栏最右侧的状态点表示前端能否连接 Lumina 后端：

- **绿色：** 最近一次 `/health` 检查返回正常；
- **红色：** 最近一次检查失败或后端不可达；
- **检查中：** 页面尚未收到首次检查结果。

软件在页面载入时立即检查一次，之后每 5 秒重新检查。绿色只证明前后端当前可以通信，不代表某个 LUT、
图片、临时会话或生成任务一定有效。红色时先确认 Lumina 后端仍在运行，再重试当前操作。

## 更新公告

启动后若存在尚未读过的新公告，软件会显示最近的更新记录。直接关闭只结束本次显示；选择“本版本不再提示”
后，当前最新公告版本会记录在本机浏览器存储中。公告阅读状态不会上传，也不会修改转换设置。

## 页面切换会保留什么

界面会保留当前控件值、已选择的页面和面板布局；设备设置还会以尽力而为的方式与后端同步。临时预览、
下载链接和文件会话仍有生命周期，不能把“切换回来还能看到”当作永久保存。需要留档时，应导出分享卡、
JSON、3MF 以及实际使用的 LUT 或耗材档案。

---
id: reference-separate-backing
title: "分离底板"
slug: "/reference/model-features/separate-backing"
description: "说明分离底板如何改变 3MF 对象结构、底板耗材归属，以及预览和切片时需要检查的内容。"
sidebar_position: 1
---

:::info AI 辅助编写说明

本文由 AI 在分析 Lumina Studio 当前源码、现有 Wiki 内容及相关操作流程后协助撰写，并已按当前版本进行基础核对。由于软件仍在持续更新，内容可能存在疏漏或与后续版本不一致。如发现不准确之处，或有更清晰的解释、案例与建议，欢迎在本页评论区反馈，或通过 GitHub 提交修订。

:::

# 分离底板

“分离底板”决定底板网格是并入底色对象，还是作为单独对象写入最终模型。它改变的是 3MF 内的对象结构，
不会自动增加底板厚度，也不会凭空增加一种耗材。

此项默认关闭。

## 控件位置

1. 进入“图像转换”。
2. 打开“核心设置”中的“高级设置”。
3. 找到“分离底板”。

<img
  src={require('./assets/01-separate-backing-location.webp').default}
  alt="Lumina Studio 高级设置中的分离底板开关"
/>

## 关闭时

底板会并入当前选定的底板耗材对象。切片软件通常把它与同一耗材的图像部分视为一个对象，材料映射较简单。

## 开启时

软件会保留独立的底板网格：

- 像素与高保真模型中，底板以独立的 `Backing` 对象写入场景。
- 原生矢量模型中，底板以独立的 `Board` 对象保留。
- 底板仍使用当前选定的底板耗材信息；“独立对象”不等于“新增耗材槽位”。
- 大尺寸分块生成时，每个有效分块都会按同一规则保留或合并底板。

导入切片软件后，应在对象列表中确认底板对象存在，并检查它映射到的耗材是否正确。不同切片软件对多对象
的自动分组方式不同，不能只看工作台上的预览颜色判断。

## 预览与重新生成

普通 2D 颜色预览主要显示颜色匹配结果，不用它判断底板是否已经分离。最终对象结构以重新生成后的 3MF
和切片软件对象列表为准。

切换此项后请重新生成模型。该设置会随 Lumina 配方卡保存。

## 适用场景

- 需要在切片软件里单独选择、隐藏或检查底板。
- 需要把底板作为独立部件进行后处理。
- 打印机或切片工作流要求底板与图像颜色对象分开管理。

如果不需要单独处理底板，关闭此项通常能得到更简单的对象列表。

## 相关页面

- [底色层高与底板厚度](../../../knowledge/base-color-layer-height/base-color-layer-height.mdx)
- [切片预览、软件预览与实物为什么不同](../../../knowledge/preview-slicer-print-differences/preview-slicer-print-differences.mdx)
- [切片软件中的耗材映射](../../../tutorials/slicer-material-mapping/slicer-material-mapping.mdx)
